//Verifica se o DOM carregou
document.addEventListener("DOMContentLoaded", function(event) { 
// carregou o DOM
  
  //Manipula o evento click
  element.onclick = (evt) => {};
  
  //Manipula o evento change
  element.onchange = (evt) => {};
  
  //Manipula o evento focus
  element.onfocus = (evt) => {};
  
  //Manipula o evento mouse enter
  element.onmouseenter = (evt) => {};
  
  //Manipula o evento key press
  element.onkeypress = (evt) => {};
  
  //Manipula o evento submit
  element.onsubmit = (evt) => {};
})

